#pragma once

//
// Minimal required typedefs and macros to avoid including Windows.h in shared headers
//

#define NULL ((void*)0)

#define FALSE 0
#define TRUE 1

typedef void *HANDLE;
typedef int BOOL;
typedef unsigned long DWORD;
typedef unsigned short WORD;
typedef unsigned char BYTE;
